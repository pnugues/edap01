This folder contains two linear regression programs, one using NumPy and the other with vector operations I implemented.  
The NumPy version is shorter and faster, but it relies on NumPy. With the other version, you can unterstand better how to implement basic vector and matrix operations.
